{
  "name": "hos-lab",
  "version": "0.2.0-beta",
  "private": true,
  "type": "module",
  "description": "Black-box scored benchmark for HOS: install, adopt, upgrade, orchestration, retrieval, autonomy, audit, tasks, and process efficiency.",
  "scripts": {
    "run": "node src/cli.mjs run",
    "test": "node --test",
    "clean": "node src/cli.mjs clean"
  },
  "engines": {
    "node": ">=18"
  }
}
