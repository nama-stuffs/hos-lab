{
  "name": "hos-lab",
  "version": "0.1.0-beta",
  "private": true,
  "type": "module",
  "description": "Black-box scored benchmark for HOS: install, adopt, upgrade, orchestration, retrieval, and process efficiency.",
  "scripts": {
    "run": "node src/cli.mjs run",
    "test": "node --test",
    "clean": "node src/cli.mjs clean"
  },
  "engines": {
    "node": ">=18"
  }
}
