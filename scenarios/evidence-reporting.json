{
  "id": "evidence-reporting",
  "fixture": "evidence-reporting",
  "phase": "report",
  "classification": "report",
  "candidateHosArea": ".hos/tools/lib/report.mjs",
  "commands": [
    ["init", "--name", "Lab Evidence"],
    ["workflow", "start", "Evidence request", "--title", "Evidence flow", "--acceptance", "report and metrics work", "--actor", "backend", "--level", "high"],
    ["workflow", "plan", "${command.1.ticket}", "--execute", "backend", "--verify", "rev+tester", "--evidence", "captured command proof"],
    ["ticket", "move", "${command.1.ticket}", "reproduced"],
    ["compose", "backend", "--ticket", "${command.1.ticket}"],
    ["run", "${command.1.ticket}", "--by", "backend", "--", "echo", "EVIDENCE"],
    ["ticket", "move", "${command.1.ticket}", "fixed"],
    ["session", "open", "Verify the evidence flow"],
    ["compose", "rev+tester", "--ticket", "${command.1.ticket}"],
    ["ticket", "verify", "${command.1.ticket}", "--result", "pass", "--by", "rev+tester", "--step", "s2", "--evidence", "run log"],
    ["ticket", "move", "${command.1.ticket}", "verified"],
    ["retro", "${command.1.ticket}", "--outcome", "no-op"],
    ["workflow", "lint", "${command.1.ticket}"],
    ["session", "close", "${command.1.session}", "--summary", "Done"],
    ["report", "${command.1.session}", "--format", "md"],
    ["metrics", "ticket", "${command.1.ticket}"]
  ],
  "assertions": [
    { "type": "jsonField", "command": 12, "path": "ok", "equals": true },
    { "type": "commandPathExists", "command": 14, "path": "0" },
    { "type": "fileFromCommandContains", "command": 14, "path": "0", "text": "## Optimization" },
    { "type": "jsonField", "command": 15, "path": "status", "equals": "verified" },
    { "type": "jsonField", "command": 15, "path": "verifyPass", "equals": 1 },
    { "type": "jsonField", "command": 15, "path": "retrospective", "equals": true },
    { "type": "jsonArrayIncludes", "command": 15, "path": "retroOutcomes", "includes": "no-op" }
  ]
}
